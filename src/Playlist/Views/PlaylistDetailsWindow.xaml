<Window x:Class="Playlist.Views.PlaylistDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Playback Mode" Height="320" Width="520" MinHeight="280" MinWidth="460"
        Icon="/Assets/icon.ico"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Name="PlaylistNameText"
                   Grid.Row="0"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Select Mode"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6" />
            <ComboBox Name="PlaybackModeComboBox"
                      SelectionChanged="PlaybackModeComboBox_SelectionChanged"
                      MinWidth="260" />
        </StackPanel>

        <Border Grid.Row="2" Background="#F7F7F7" CornerRadius="6" Padding="10">
            <StackPanel>
                <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock Name="ModeDescriptionText"
                           TextWrapping="Wrap"
                           Foreground="#444" />
            </StackPanel>
        </Border>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Name="SaveStatusText"
                       Text=""
                       Foreground="#2E7D32"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left" />
            <Button Content="Close"
                    Width="90"
                    Height="30"
                    HorizontalAlignment="Right"
                    Click="Close_Click"
                    DockPanel.Dock="Right" />
        </DockPanel>
    </Grid>
</Window>
